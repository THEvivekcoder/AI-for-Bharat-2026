# Core FastAPI dependencies
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.3
pydantic-settings>=2.1.0

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.10
alembic==1.13.1

# Redis
redis==5.0.1

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
pyotp==2.9.0

# Utilities
python-dotenv==1.0.0
phonenumbers==8.13.27

# Voice Processing
# Note: Install these separately if needed for full functionality
# For now, using lighter alternatives
# openai-whisper  # Requires: pip install setuptools first, then: pip install openai-whisper
# torch>=2.0.0
# torchaudio>=2.0.0
librosa==0.10.1
soundfile==0.12.1
pydub==0.25.1
numpy>=1.26.0
gTTS==2.5.0
# For speech recognition alternative (lighter than Whisper)
SpeechRecognition==3.10.1

# RAG and LLM dependencies
sentence-transformers==2.3.1
faiss-cpu>=1.7.4
langchain==0.1.0
langchain-community==0.0.13
openai==1.7.2

# Language Processing
googletrans==4.0.0rc1
langdetect==1.0.9

# Testing
pytest==7.4.3
hypothesis==6.92.1
psutil==5.9.8  # For performance monitoring
